import setuptools

setuptools.setup(
    name='allsky',
    version='0.0.0',
    description='Software to control the allsky camera of the Cereceda observatory',
    packages=setuptools.find_packages(),
    package_data={"allsky" :[],}
)
